# AppCompiler
AppCompiler is a system that:  Extracts intent from user prompts Designs the app architecture Generates UI, API, DB, Auth schemas Validates &amp; repairs inconsistencies Output: Production-ready JSON config
